apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rabbit-mq-pvc
  namespace: real-time-chat-app
spec:
  storageClassName: "local-path"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
